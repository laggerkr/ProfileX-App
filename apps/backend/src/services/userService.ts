import type { AuthUser, Role } from "@profilex/shared"; import type { AppDatabase } from "../database/db.js"; import { logActivity } from "./activityService.js";
const map=(r:any):AuthUser=>({id:r.id,name:r.name,email:r.email,role:r.member_role??r.role,organizationId:r.organization_id,lastLoginAt:r.last_login_at??undefined,status:r.member_status??r.status,createdAt:r.created_at});
export async function listUsers(db:AppDatabase,orgId:string){return (await db.query(`SELECT u.*,om.organization_id,om.role AS member_role,om.status AS member_status FROM users u JOIN organization_members om ON om.user_id=u.id WHERE om.organization_id=$1 ORDER BY u.created_at`,[orgId])).map(map)}
export async function updateUser(db:AppDatabase,orgId:string,id:string,patch:{role?:Role;status?:string},actorId:string){const row=await db.one<any>('SELECT * FROM organization_members WHERE organization_id=$1 AND user_id=$2',[orgId,id]); if(!row)return undefined; const role=patch.role??row.role,status=patch.status??row.status; await db.exec('UPDATE organization_members SET role=$1,status=$2 WHERE organization_id=$3 AND user_id=$4',[role,status,orgId,id]); await db.exec('UPDATE users SET role=$1,status=$2 WHERE id=$3',[role,status,id]); await logActivity(db,'user.role_changed',id,actorId); return (await listUsers(db,orgId)).find(u=>u.id===id)}
export async function deleteUser(db:AppDatabase,orgId:string,id:string,actorId:string){const row=await db.one('SELECT 1 FROM organization_members WHERE organization_id=$1 AND user_id=$2',[orgId,id]); if(!row)return false; await db.exec('DELETE FROM organization_members WHERE organization_id=$1 AND user_id=$2',[orgId,id]); await logActivity(db,'user.removed',id,actorId); return true}
