import { contextBridge, ipcRenderer } from "electron"; const channels={launch:"profilex:launch-profile",stop:"profilex:stop-profile",export:"profilex:export-profile-state"} as const; contextBridge.exposeInMainWorld("profilex",{platform:process.platform,apiBaseUrl:"http://127.0.0.1:4387/api",launchProfile:(args:unknown)=>ipcRenderer.invoke(channels.launch,args),stopProfile:(id:string)=>ipcRenderer.invoke(channels.stop,id),exportProfileState:(id:string)=>ipcRenderer.invoke(channels.export,id)});
